<div class="small-container center-block bg-nav"> 
<div class="margin-top p-5">

<h2 class="text-uppercase text-info font-weight-bold">olvide mi contraseña</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email, "correo electronico" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions">
    <%= f.submit "Continuar", class:"btn btn-primary mt-3" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>